• Automate HTTP verb testing across discovered endpoints
  Script repeated requests with different HTTP methods (POST, PUT, DELETE, PATCH) to identify unprotected or sensitive operations on /api/process and /api/user endpoints.

• Capture and replay requests via HTTP proxy
  Use Burp Suite or similar to intercept, inspect, and modify requests before delivery—enabling rapid payload iteration and response analysis.

• Document API schema and response formats
  Record observed endpoint paths, accepted methods, response codes, and content types to inform injection payload design.
